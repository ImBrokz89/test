if getgenv().ReinjectLoaded then return end
getgenv().ReinjectLoaded = true

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer
local HttpService = game:GetService("HttpService")

-- 🔧 Déclaration des variables globales
local Character = nil
local HRP = nil

local ScriptURL = "https://raw.githubusercontent.com/ImBrokz89/test/refs/heads/main/dqtest"

local queue = queue_on_teleport 
           or (syn and syn.queue_on_teleport) 
           or (fluxus and fluxus.queue_on_teleport) 
           or queueonteleport

if not queue then
    print("❌ Queue on teleport non dispon")
    return
end

local TeleportQueued = false

LocalPlayer.OnTeleport:Connect(function()
    if TeleportQueued then return end
    TeleportQueued = true

    queue([[
        repeat task.wait() until game:IsLoaded()
        task.wait(2)
        loadstring(game:HttpGet("]]..ScriptURL..[[", true))()
    ]])
end)

--------------------------------------------------
-- RÉCUPÉRER LE PLACE ID
--------------------------------------------------

local TARGET_PLACE_ID = "77649408247578"
local currentPlaceId = tostring(game.PlaceId)

print("📍 Place ID actuel:", currentPlaceId)
print("🎯 Place ID cible:", TARGET_PLACE_ID)

local canCreateDungeon = (currentPlaceId == TARGET_PLACE_ID)

if canCreateDungeon then
    print("✅ Place ID valide ! Création du donjon autorisée.")
else
    print("⚠️ Place ID différent, le script continue mais ne créera pas de donjon.")
end

--------------------------------------------------
-- FONCTIONS DE LECTURE/ÉCRITURE DE FICHIER
--------------------------------------------------

local function FileExists(filename)
    if isfile then
        return isfile(filename)
    end
    return false
end

local function ReadFile(filename)
    if isfile then
        return readfile(filename)
    end
    return nil
end

local function WriteFile(filename, content)
    if writefile then
        writefile(filename, content)
        return true
    end
    return false
end

--------------------------------------------------
-- FONCTION POUR FORMATER LE JSON
--------------------------------------------------

local function FormatJSON(data)
    local json = HttpService:JSONEncode(data)
    
    json = string.gsub(json, "{", "{\n  ")
    json = string.gsub(json, "}", "\n}")
    json = string.gsub(json, ",", ",\n  ")
    json = string.gsub(json, "\n  \n", "\n")
    
    return json
end

--------------------------------------------------
-- CRÉER LE DOSSIER ET LE FICHIER CONFIG DANS Workspace
--------------------------------------------------

local function CreateConfigInWorkspace()
    print("📁 Création du dossier Omega.Win dans Workspace...")
    
    local omegaFolder = workspace:FindFirstChild("Omega.Win")
    if not omegaFolder then
        omegaFolder = Instance.new("Folder")
        omegaFolder.Name = "Omega.Win"
        omegaFolder.Parent = workspace
        print("✅ Dossier Omega.Win créé dans Workspace")
    else
        print("✅ Dossier Omega.Win déjà existant")
    end
    
    local defaultConfig = {
        dungeonName = "Pirate Island",
        dungeonDifficulty = "Insane",
        AutoReplay = false
    }
    
    local jsonString = FormatJSON(defaultConfig)
    
    print("📝 JSON généré:")
    print(jsonString)
    
    local configFile = omegaFolder:FindFirstChild("Config.json")
    if not configFile then
        configFile = Instance.new("StringValue")
        configFile.Name = "Config.json"
        configFile.Value = jsonString
        configFile.Parent = omegaFolder
        print("✅ Fichier Config.json créé dans Omega.Win")
    else
        configFile.Value = jsonString
        print("✅ Fichier Config.json mis à jour")
    end
    
    print("📋 Configuration:")
    print("  📌 Dungeon Name:", defaultConfig.dungeonName)
    print("  📌 Dungeon Difficulty:", defaultConfig.dungeonDifficulty)
    print("  📌 AutoReplay:", defaultConfig.AutoReplay)
    
    return defaultConfig
end

--------------------------------------------------
-- LIRE LA CONFIGURATION DEPUIS Workspace
--------------------------------------------------

local function LoadConfigFromWorkspace()
    local config = {
        DUNGEON_NAME = "Pirate Island",
        DUNGEON_DIFFICULTY = "Insane",
        AUTO_REPLAY = false
    }
    
    local omegaFolder = workspace:FindFirstChild("Omega.Win")
    if not omegaFolder then
        print("⚠️ Dossier Omega.Win introuvable, création...")
        return CreateConfigInWorkspace()
    end
    
    local configFile = omegaFolder:FindFirstChild("Config.json")
    if not configFile then
        print("⚠️ Fichier Config.json introuvable, création...")
        return CreateConfigInWorkspace()
    end
    
    if configFile:IsA("StringValue") then
        local content = configFile.Value
        
        local success, decoded = pcall(function()
            return HttpService:JSONDecode(content)
        end)
        
        if success and decoded then
            print("✅ Configuration chargée depuis Workspace/Omega.Win/Config.json")
            print("📋 Contenu du fichier:")
            print(content)
            
            if decoded.dungeonName then 
                config.DUNGEON_NAME = decoded.dungeonName 
            end
            if decoded.dungeonDifficulty then 
                config.DUNGEON_DIFFICULTY = decoded.dungeonDifficulty 
            end
            if decoded.AutoReplay ~= nil then
                config.AUTO_REPLAY = decoded.AutoReplay
            end
            
            print("  📌 Dungeon Name:", config.DUNGEON_NAME)
            print("  📌 Dungeon Difficulty:", config.DUNGEON_DIFFICULTY)
            print("  📌 AutoReplay:", config.AUTO_REPLAY)
        else
            print("❌ Erreur de lecture du JSON, utilisation des valeurs par défaut")
            print("📝 Contenu actuel du fichier:", content)
        end
    end
    
    return config
end

--------------------------------------------------
-- ATTENDRE LE SPAWN
--------------------------------------------------

repeat
    task.wait(0.1)
    Character = LocalPlayer.Character
until Character
    and Character:FindFirstChild("HumanoidRootPart")
    and Character:FindFirstChildOfClass("Humanoid")
task.wait(1)

HRP = Character:FindFirstChild("HumanoidRootPart")

--------------------------------------------------
-- CHARGER OU CRÉER LA CONFIGURATION DANS WORKSPACE
--------------------------------------------------

local Config = LoadConfigFromWorkspace()

--------------------------------------------------
-- DÉPENSER LES POINTS DE COMPÉTENCE
--------------------------------------------------

local function SpendSkillPoints()
    local skillPoints = game:GetService("Players").LocalPlayer:FindFirstChild("skillPoints")
    
    if not skillPoints then 
        print("❌ skillPoints introuvable")
        return 
    end
    
    if skillPoints.Value > 0 then
        print("🔧 Dépense de " .. skillPoints.Value .. " points de compétence")
        
        local Event = game:GetService("ReplicatedStorage").remotes:FindFirstChild("spendSkillPoint")
        if Event then
            for i = 1, skillPoints.Value do
                pcall(function()
                    Event:FireServer("physicalPower", 1)
                    print("✅ Point de compétence dépensé (" .. i .. "/" .. skillPoints.Value .. ")")
                end)
                task.wait(0.1)
            end
        else
            print("❌ spendSkillPoint introuvable")
        end
    else
        print("ℹ️ Aucun point de compétence à dépenser")
    end
end

task.wait(2)
SpendSkillPoints()

--------------------------------------------------
-- VÉRIFIER SI ON EST DANS LE LOBBY
--------------------------------------------------

local function IsInLobby()
    local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
    if not playerGui then return false end
    
    local introGui = playerGui:FindFirstChild("introGui")
    if not introGui then return false end
    
    if introGui:IsA("ScreenGui") then
        return introGui.Enabled
    end
    
    return false
end

local function IsStartButtonVisible()
    local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
    if not playerGui then return false end
    
    local startButton = nil
    for _, gui in ipairs(playerGui:GetChildren()) do
        if gui:IsA("ScreenGui") then
            startButton = gui:FindFirstChild("startButton")
            if startButton then break end
        end
    end
    
    if not startButton then
        startButton = playerGui:FindFirstChild("startButton")
    end
    
    if not startButton then
        return false
    end
    
    if startButton:IsA("ScreenGui") then
        return startButton.Enabled
    elseif startButton:IsA("GuiObject") then
        return startButton.Visible
    end
    
    return false
end

--------------------------------------------------
-- AUTO REPLAY
--------------------------------------------------

-- Fonction pour récupérer les infos du donjon
local function GetDungeonInfo()
    local info = {
        dungeonName = "Unknown",
        dungeonProgress = "inProgress",
        dungeonFinished = false,
        dungeonStarted = false,
        isHardcore = false,
        fightingBoss = false
    }
    
    -- Récupérer le nom du donjon depuis workspace.dungeonName
    local dungeonNameValue = workspace:FindFirstChild("dungeonName")
    if dungeonNameValue and dungeonNameValue:IsA("StringValue") then
        info.dungeonName = dungeonNameValue.Value
    end
    
    -- Récupérer la progression du donjon depuis workspace.dungeonProgress
    local dungeonProgressValue = workspace:FindFirstChild("dungeonProgress")
    if dungeonProgressValue and dungeonProgressValue:IsA("StringValue") then
        info.dungeonProgress = dungeonProgressValue.Value
    end
    
    -- Récupérer dungeonFinished depuis workspace.dungeon.bossRoom.dungeonFinished
    local dungeon = workspace:FindFirstChild("dungeon")
    if dungeon then
        local bossRoom = dungeon:FindFirstChild("bossRoom")
        if bossRoom then
            local dungeonFinishedValue = bossRoom:FindFirstChild("dungeonFinished")
            if dungeonFinishedValue and dungeonFinishedValue:IsA("BoolValue") then
                info.dungeonFinished = dungeonFinishedValue.Value
            end
        end
    end
    
    -- Récupérer dungeonStarted depuis workspace.start
    local startValue = workspace:FindFirstChild("start")
    if startValue and startValue:IsA("BoolValue") then
        info.dungeonStarted = startValue.Value
    end
    
    -- Récupérer isHardcore depuis workspace.hardcore
    local hardcoreValue = workspace:FindFirstChild("hardcore")
    if hardcoreValue and hardcoreValue:IsA("BoolValue") then
        info.isHardcore = hardcoreValue.Value
    end
    
    return info
end

-- Fonction pour vérifier si le bouton Replay est visible
local function IsReplayButtonVisible()
    local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
    if not playerGui then return false end
    
    local mainInterface = playerGui:FindFirstChild("mainInterface")
    if not mainInterface then return false end
    
    local buttons = mainInterface:FindFirstChild("buttons")
    if not buttons then return false end
    
    local optionsButton = buttons:FindFirstChild("optionsButton")
    if not optionsButton then return false end
    
    local replayButton = optionsButton:FindFirstChild("ReplayDungeonButton")
    if not replayButton then return false end
    
    -- Vérifier si le bouton est visible
    if replayButton:IsA("GuiObject") then
        return replayButton.Visible
    end
    
    return false
end

-- Fonction pour lancer le replay
local function ReplayDungeon()
    print("🔄 Lancement du Auto Replay...")
    
    local dungeonInfo = GetDungeonInfo()
    
    print("📋 Infos du donjon:")
    print("  📌 Nom:", dungeonInfo.dungeonName)
    print("  📌 Progression:", dungeonInfo.dungeonProgress)
    print("  📌 Dungeon Started:", dungeonInfo.dungeonStarted)
    print("  📌 Dungeon Finished:", dungeonInfo.dungeonFinished)
    print("  📌 Hardcore:", dungeonInfo.isHardcore)
    
    -- Vérifier si le donjon est fini ou a échoué
    local shouldReplay = false
    
    if dungeonInfo.dungeonProgress == "bossKilled" then
        print("✅ Boss tué ! Auto Replay déclenché.")
        shouldReplay = true
    elseif dungeonInfo.dungeonProgress == "hardcoreFailed" then
        print("💀 Hardcore échoué ! Auto Replay déclenché.")
        shouldReplay = true
    elseif dungeonInfo.dungeonFinished == true then
        print("✅ Donjon terminé ! Auto Replay déclenché.")
        shouldReplay = true
    end
    
    if not shouldReplay then
        print("⏭️ Donjon pas encore fini, pas de replay.")
        return
    end
    
    -- Vérifier si le bouton Replay est visible
    if not IsReplayButtonVisible() then
        print("⚠️ Bouton Replay non visible, impossible de lancer le replay.")
        return
    end
    
    print("🟢 Bouton Replay visible ! Lancement...")
    
    -- Préparer les données pour le replay
    local replayData = {
        dungeonProgress = dungeonInfo.dungeonProgress,
        dungeonStarted = dungeonInfo.dungeonStarted,
        hardcore = dungeonInfo.isHardcore,
        dungeonFinished = dungeonInfo.dungeonFinished,
        dungeonName = dungeonInfo.dungeonName,
        isHardcore = dungeonInfo.isHardcore,
        fightingBoss = false
    }
    
    -- Envoyer l'event replayDungeon
    local Event = ReplicatedStorage:FindFirstChild("remotes")
    if Event then
        local replayEvent = Event:FindFirstChild("replayDungeon")
        if replayEvent then
            pcall(function()
                replayEvent:FireServer(replayData)
                print("✅ Replay envoyé !")
                print("📋 Données envoyées:")
                print("  📌 dungeonProgress:", replayData.dungeonProgress)
                print("  📌 dungeonStarted:", replayData.dungeonStarted)
                print("  📌 hardcore:", replayData.hardcore)
                print("  📌 dungeonFinished:", replayData.dungeonFinished)
                print("  📌 dungeonName:", replayData.dungeonName)
                print("  📌 isHardcore:", replayData.isHardcore)
                print("  📌 fightingBoss:", replayData.fightingBoss)
            end)
        else
            print("❌ replayDungeon introuvable")
        end
    end
end

-- Fonction de surveillance Auto Replay
local function StartAutoReplay()
    print("👀 Surveillance Auto Replay activée")
    
    while true do
        -- Vérifier si l'Auto Replay est activé dans la config
        if Config.AUTO_REPLAY then
            local dungeonInfo = GetDungeonInfo()
            
            -- Vérifier si le donjon est fini ou a échoué
            local shouldReplay = false
            
            if dungeonInfo.dungeonProgress == "bossKilled" then
                shouldReplay = true
            elseif dungeonInfo.dungeonProgress == "hardcoreFailed" then
                shouldReplay = true
            elseif dungeonInfo.dungeonFinished == true then
                shouldReplay = true
            end
            
            if shouldReplay then
                -- Vérifier si le bouton Replay est visible
                if IsReplayButtonVisible() then
                    ReplayDungeon()
                    -- Attendre un peu après le replay
                    task.wait(5)
                end
            end
        end
        
        task.wait(2) -- Vérifier toutes les 2 secondes
    end
end

--------------------------------------------------
-- CRÉER LE DONJON
--------------------------------------------------

local function CreateDungeon()
    if not canCreateDungeon then
        print("⏭️ Création de donjon ignorée (Place ID incorrect)")
        return false
    end
    
    print("🚀 Création du donjon avec la configuration:")
    print("  📌 Nom:", Config.DUNGEON_NAME)
    print("  📌 Difficulté:", Config.DUNGEON_DIFFICULTY)
    print("  📌 Place ID:", TARGET_PLACE_ID)
    
    local LobbyRemotes = ReplicatedStorage:FindFirstChild("remotes")
    if not LobbyRemotes then
        print("❌ remotes introuvable")
        return false
    end
    
    local CreateLobby = LobbyRemotes:FindFirstChild("createLobby")
    if not CreateLobby then
        print("❌ createLobby introuvable")
        return false
    end
    
    local success, result = pcall(function()
        return CreateLobby:InvokeServer(
            Config.DUNGEON_NAME,
            Config.DUNGEON_DIFFICULTY,
            0,
            false,
            false,
            false
        )
    end)
    
    if success then
        print("✅ Donjon créé avec succès")
        return true
    else
        print("❌ Erreur lors de la création du donjon:", result)
        return false
    end
end

local function StartDungeonRemote()
    if not canCreateDungeon then
        print("⏭️ Démarrage de donjon ignoré (Place ID incorrect)")
        return false
    end
    
    print("🚀 Démarrage du donjon...")
    
    local LobbyRemotes = ReplicatedStorage:FindFirstChild("remotes")
    if not LobbyRemotes then
        print("❌ remotes introuvable")
        return false
    end
    
    local StartDungeon = LobbyRemotes:FindFirstChild("startDungeon")
    if not StartDungeon then
        print("❌ startDungeon introuvable")
        return false
    end
    
    pcall(function()
        StartDungeon:FireServer()
        print("✅ Donjon démarré")
    end)
    
    return true
end

--------------------------------------------------
-- SURVEILLER LES CHANGEMENTS DE CONFIG DANS WORKSPACE
--------------------------------------------------

task.spawn(function()
    print("👀 Surveillance du fichier Workspace/Omega.Win/Config.json activée")
    
    while true do
        local omegaFolder = workspace:FindFirstChild("Omega.Win")
        if omegaFolder then
            local configFile = omegaFolder:FindFirstChild("Config.json")
            if configFile and configFile:IsA("StringValue") then
                local success, decoded = pcall(function()
                    return HttpService:JSONDecode(configFile.Value)
                end)
                
                if success and decoded then
                    local hasChanged = false
                    
                    if decoded.dungeonName and decoded.dungeonName ~= Config.DUNGEON_NAME then
                        Config.DUNGEON_NAME = decoded.dungeonName
                        hasChanged = true
                        print("🔄 Dungeon Name mis à jour:", Config.DUNGEON_NAME)
                    end
                    
                    if decoded.dungeonDifficulty and decoded.dungeonDifficulty ~= Config.DUNGEON_DIFFICULTY then
                        Config.DUNGEON_DIFFICULTY = decoded.dungeonDifficulty
                        hasChanged = true
                        print("🔄 Dungeon Difficulty mise à jour:", Config.DUNGEON_DIFFICULTY)
                    end
                    
                    if decoded.AutoReplay ~= nil and decoded.AutoReplay ~= Config.AUTO_REPLAY then
                        Config.AUTO_REPLAY = decoded.AutoReplay
                        hasChanged = true
                        print("🔄 AutoReplay mis à jour:", Config.AUTO_REPLAY)
                    end
                    
                    if hasChanged then
                        print("📋 Nouvelle configuration chargée depuis Workspace!")
                    end
                end
            end
        end
        task.wait(2)
    end
end)

--------------------------------------------------
-- ATTENDRE LE LOBBY
--------------------------------------------------

if canCreateDungeon then
    print("🔍 Attente du lobby...")
    local waitTime = 0
    local maxWait = 10

    while not IsInLobby() and waitTime < maxWait do
        task.wait(1)
        waitTime = waitTime + 1
        print("⏳ Attente du lobby... (" .. waitTime .. "s)")
    end

    if IsInLobby() then
        print("✅ Lobby détecté !")
    else
        print("⚠️ Lobby non détecté après " .. maxWait .. "s, création quand même...")
    end

    CreateDungeon()
    task.wait(2)
    StartDungeonRemote()
else
    print("⏭️ Étape de création du donjon sautée (Place ID incorrect)")
end

--------------------------------------------------
-- ATTENDRE LE BOUTON START
--------------------------------------------------

if canCreateDungeon then
    local function WaitForStartButton()
        print("🔍 Attente du bouton START...")
        local waitTime = 0
        local maxWait = 60
        
        while not IsStartButtonVisible() and waitTime < maxWait do
            task.wait(0.5)
            waitTime = waitTime + 0.5
            if waitTime % 2 == 0 then
                print("⏳ Attente du bouton START... (" .. waitTime .. "s)")
            end
        end
        
        if IsStartButtonVisible() then
            print("✅ Bouton START détecté !")
            return true
        else
            print("⚠️ Bouton START non détecté après " .. maxWait .. "s")
            return false
        end
    end

    local startButtonFound = WaitForStartButton()

    if startButtonFound then
        print("🚀 Lancement de l'event START...")
        
        local Remotes = ReplicatedStorage:FindFirstChild("remotes")
        if Remotes then
            local ChangeStartValue = Remotes:FindFirstChild("changeStartValue")
            if ChangeStartValue then
                pcall(function()
                    ChangeStartValue:FireServer()
                    print("✅ changeStartValue envoyé")
                end)
            end
        end
    end
else
    print("⏭️ Attente du bouton START sautée (Place ID incorrect)")
end

--------------------------------------------------
-- SURVEILLANCES
--------------------------------------------------

task.spawn(function()
    print("👀 Surveillance du bouton START activée")
    local startButtonWasVisible = false
    
    while true do
        local isVisible = IsStartButtonVisible()
        
        if isVisible and not startButtonWasVisible then
            print("🟢 Bouton START détecté ! Lancement de l'event...")
            
            local Remotes = ReplicatedStorage:FindFirstChild("remotes")
            if Remotes then
                local ChangeStartValue = Remotes:FindFirstChild("changeStartValue")
                if ChangeStartValue then
                    pcall(function()
                        ChangeStartValue:FireServer()
                        print("✅ changeStartValue envoyé")
                    end)
                end
            end
            
            startButtonWasVisible = true
        end
        
        if not isVisible and startButtonWasVisible then
            print("🔴 Bouton START a disparu")
            startButtonWasVisible = false
        end
        
        task.wait(0.5)
    end
end)

task.spawn(function()
    local skillPoints = game:GetService("Players").LocalPlayer:FindFirstChild("skillPoints")
    
    if skillPoints then
        skillPoints:GetPropertyChangedSignal("Value"):Connect(function()
            if skillPoints.Value > 0 then
                print("🆕 Nouveaux points de compétence détectés :", skillPoints.Value)
                task.wait(0.5)
                SpendSkillPoints()
            end
        end)
        print("👀 Surveillance des points de compétence activée")
    end
end)

task.spawn(function()
    print("👀 Surveillance du lobby (introGui) activée")
    local lobbyWasActive = false
    
    while true do
        local isInLobby = IsInLobby()
        
        if isInLobby and not lobbyWasActive then
            print("🔄 Lobby détecté (introGui visible) !")
            lobbyWasActive = true
        end
        
        if not isInLobby and lobbyWasActive then
            print("🔴 Lobby a disparu (introGui caché)")
            lobbyWasActive = false
        end
        
        task.wait(1)
    end
end)

--------------------------------------------------
-- DÉMARRER L'AUTO REPLAY
--------------------------------------------------

-- Démarrer la surveillance Auto Replay
task.spawn(StartAutoReplay)

--------------------------------------------------
-- VARIABLES DE COMBAT
--------------------------------------------------

local Offset = 5
local SwingDelay = 0.1
local isAttacking = false
local SLIDE_SPEED = 32
local SWING_RANGE = 10

--------------------------------------------------
-- RÉCUPÉRER LES ENNEMIS
--------------------------------------------------

local function GetEnemies()
    local enemies = {}
    
    for _, folder in ipairs(workspace:GetDescendants()) do
        if folder:IsA("Folder") and folder.Name == "enemyFolder" then
            for _, enemy in ipairs(folder:GetChildren()) do
                if enemy:IsA("Model") then
                    local humanoid = enemy:FindFirstChildOfClass("Humanoid")
                    if humanoid and humanoid.Health > 0 then
                        if enemy ~= LocalPlayer.Character then
                            table.insert(enemies, enemy)
                        end
                    end
                end
            end
        end
    end
    
    return enemies
end

--------------------------------------------------
-- RÉCUPÉRER LE HRP
--------------------------------------------------

local function GetHRP(model)
    if not model then return nil end
    return model:FindFirstChild("HumanoidRootPart")
        or model:FindFirstChild("Torso")
        or model:FindFirstChild("UpperTorso")
end

--------------------------------------------------
-- POSITIONNER LE PERSONNAGE
--------------------------------------------------

local function SetProne(pos, lookPos)
    if not HRP or not HRP.Parent then 
        return 
    end

    local direction = lookPos - pos
    if direction.Magnitude <= 0.01 then 
        return 
    end

    local look = direction.Unit
    local up = Vector3.new(0, 1, 0)
    
    local cf = CFrame.lookAt(pos, pos + look, up)
    
    HRP.CFrame = cf
    HRP.AssemblyLinearVelocity = Vector3.zero
end

--------------------------------------------------
-- RÉCUPÉRER L'ARME ÉQUIPÉE
--------------------------------------------------

local function GetEquippedWeapon()
    local character = LocalPlayer.Character
    if not character then return nil end
    
    local weaponValue = LocalPlayer:FindFirstChild("weaponEquipped")
    if weaponValue then
        if weaponValue:IsA("ObjectValue") then
            return weaponValue.Value
        elseif weaponValue:IsA("StringValue") then
            return character:FindFirstChild(weaponValue.Value)
        end
    end
    
    local dualRight = character:FindFirstChild("dualRight")
    if dualRight then
        return dualRight
    end
    
    for _, child in ipairs(character:GetChildren()) do
        if child:IsA("Tool") then
            local swing = child:FindFirstChild("swing")
            if swing and swing:IsA("RemoteEvent") then
                return child
            end
        end
    end
    
    return nil
end

--------------------------------------------------
-- ATTAQUE (SWING)
--------------------------------------------------

local function SwingWeapon()
    local weapon = GetEquippedWeapon()
    if not weapon then return end
    
    local swing = weapon:FindFirstChild("swing")
    if swing and swing:IsA("RemoteEvent") then
        pcall(function()
            swing:FireServer()
        end)
    end
end

--------------------------------------------------
-- UTILISER LES SORTS
--------------------------------------------------

local function UseSpellsDuringSlide(enemyPosition)
    if not HRP or not HRP.Parent then return end
    if not enemyPosition then return end
    
    local distance = (HRP.Position - enemyPosition).Magnitude
    if distance > SWING_RANGE then return end
    
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    if not backpack then return end
    
    local abilityUsed = ReplicatedStorage:FindFirstChild("remotes")
    if abilityUsed then
        abilityUsed = abilityUsed:FindFirstChild("abilityUsed")
    end
    if not abilityUsed then return end
    
    for _, tool in ipairs(backpack:GetChildren()) do
        if tool:IsA("Tool") then
            local abilitySlot = tool:FindFirstChild("abilitySlot")
            
            if abilitySlot then
                local slot = string.lower(tostring(abilitySlot.Value))
                
                if slot == "q" or slot == "e" then
                    local cooldown = tool:FindFirstChild("cooldown")
                    local ready = true
                    if cooldown then
                        local value = cooldown.Value
                        if cooldown:IsA("BoolValue") then
                            ready = value == false
                        elseif typeof(value) == "number" then
                            ready = value <= 0
                        end
                    end
                    
                    if ready then
                        pcall(function()
                            abilityUsed:FireServer(slot, tool)
                            
                            local spellEvent = tool:FindFirstChild("spellEvent")
                            if spellEvent and spellEvent:IsA("RemoteEvent") then
                                spellEvent:FireServer()
                            end
                            
                            local abilityEvent = tool:FindFirstChild("abilityEvent")
                            if abilityEvent and abilityEvent:IsA("RemoteEvent") then
                                abilityEvent:FireServer()
                            end
                        end)
                    end
                end
            end
        end
    end
end

--------------------------------------------------
-- SE DÉPLACER VERS L'ENNEMI
--------------------------------------------------

local function SlideTo(enemy, startPos)
    local humanoid = enemy:FindFirstChildOfClass("Humanoid")
    local enemyHRP = GetHRP(enemy)
    
    if not humanoid or humanoid.Health <= 0 then return end
    if not enemyHRP then return end
    if not HRP or not HRP.Parent then return end

    local playerHumanoid = Character:FindFirstChildOfClass("Humanoid")
    if not playerHumanoid then return end
    
    local walkSpeed = SLIDE_SPEED
    local originalWalkSpeed = playerHumanoid.WalkSpeed
    playerHumanoid.WalkSpeed = walkSpeed

    local pos = startPos
    local swingTimer = 0
    local stuckTime = 0
    local heartbeat = RunService.Heartbeat
    local spellTimer = 0
    local spellDelay = 0.15

    while true do
        if not HRP or not HRP.Parent then break end
        
        humanoid = enemy:FindFirstChildOfClass("Humanoid")
        if not humanoid or humanoid.Health <= 0 then 
            break
        end
        
        enemyHRP = GetHRP(enemy)
        if not enemyHRP then break end
        
        local targetPos = enemyHRP.Position + Vector3.new(0, Offset, 0)
        local distance = (targetPos - pos).Magnitude
        local distanceToEnemy = (HRP.Position - enemyHRP.Position).Magnitude
        
        local dt = heartbeat:Wait()
        
        if distance > 0.5 then
            local speed = walkSpeed * 2.2
            local step = math.min(1, speed * dt / distance)
            pos = pos:Lerp(targetPos, step)
            stuckTime = 0
        else
            stuckTime = stuckTime + dt
        end
        
        SetProne(pos, enemyHRP.Position)
        
        if distanceToEnemy <= SWING_RANGE then
            swingTimer = swingTimer + dt
            if swingTimer >= SwingDelay then
                SwingWeapon()
                swingTimer = 0
            end
            
            spellTimer = spellTimer + dt
            if spellTimer >= spellDelay then
                UseSpellsDuringSlide(enemyHRP.Position)
                spellTimer = 0
            end
        end
        
        if stuckTime >= 5 then
            break
        end
    end
    
    playerHumanoid.WalkSpeed = originalWalkSpeed
end

--------------------------------------------------
-- BOUCLE PRINCIPALE
--------------------------------------------------

while true do
    local currentChar = LocalPlayer.Character
    if currentChar then
        Character = currentChar
        local newHRP = Character:FindFirstChild("HumanoidRootPart")
        if newHRP then
            HRP = newHRP
        end
    end
    
    if isAttacking or not HRP or not HRP.Parent then
        task.wait(0.5)
        continue
    end
    
    local enemies = GetEnemies()
    
    for _, enemy in ipairs(enemies) do
        if not HRP or not HRP.Parent then break end
        
        local humanoid = enemy:FindFirstChildOfClass("Humanoid")
        local enemyHRP = GetHRP(enemy)
        
        if humanoid and humanoid.Health > 0 and enemyHRP then
            isAttacking = true
            SlideTo(enemy, HRP.Position)
            isAttacking = false
        end
    end
    
    task.wait(0.5)
end
